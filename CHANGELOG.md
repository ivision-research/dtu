# 4.3.0

- Create Python bindings for the databases and the application server
- Exposed some functionality that was not previous `pub` or was just `pub (crate)`
- Made `FileStore` require `Send` and `Sync`

# 4.2.0

First public release :)
