package dtu.lib

// Automatically generated. Changes may be overwritten!

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

import dtu.databinding.ExperimentingActivityBinding

class TestAppExperimentingActivity : AppCompatActivity() {

    private lateinit var binding: ExperimentingActivityBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ExperimentingActivityBinding.inflate(layoutInflater)
        setContentView(binding.root)
        {% for btn in buttons %}
        binding.{{ btn.id }}.setOnClickListener(
            onClickListenerForActivity(this, {{ btn.target }}::class.java)
        )
        {% endfor %}

    }
}
