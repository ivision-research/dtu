<?xml version="1.0" encoding="utf-8"?>

<!-- Automatically generated. Changes may be overwritten -->

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/experimentingScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".TestAppExperimentingActivity">

    <LinearLayout
        android:id="@+id/experimentingScrollViewInnerLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        {% for button in buttons %}
        <Button
            android:id="@+id/{{ button.id }}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="{{ button.txt }}" />
        {% endfor %}

    </LinearLayout>
</ScrollView>
