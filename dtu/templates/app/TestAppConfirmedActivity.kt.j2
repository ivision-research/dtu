package {{ app_pkg }}

// Automatically generated. Changes may be overwritten!

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

import {{ app_pkg }}.databinding.ConfirmedActivityBinding

class TestAppConfirmedActivity : AppCompatActivity() {

    private lateinit var binding: ConfirmedActivityBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ConfirmedActivityBinding.inflate(layoutInflater)
        setContentView(binding.root)
        {% for btn in buttons %}
        binding.{{ btn.id }}.setOnClickListener(
            onClickListenerForActivity(this, {{ btn.target }}::class.java)
        )
        {% endfor %}
    }
}
